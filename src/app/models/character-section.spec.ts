import { CharacterSectionComponent } from './character-section';

describe('CharacterSectionComponent', () => {
  it('should create an instance', () => {
    expect(new CharacterSectionComponent()).toBeTruthy();
  });
});
